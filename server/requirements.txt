fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.0
pydantic-settings>=2.0

# 時系列データストレージ（Ecowitt / AMeDAS 観測データ）
pyarrow>=15.0
pandas>=2.1
numpy>=1.26

# NetCDF / xarray (ERA5 気候統計)
xarray>=2024.1
netCDF4>=1.7

# ERA5 from AWS S3 (world clock — e5.oper.fc.sfc.minmax/accumu, 0.25° global)
s3fs>=2024.1
h5netcdf>=1.3
h5py>=3.10

# Google Earth Engine (AgERA5 agrometeorological indicators, 0.1°)
earthengine-api>=1.4

# Sentinel-2 via STAC (vegetation indices — Element 84 Earth Search)
pystac-client>=0.8
rasterio>=1.4
